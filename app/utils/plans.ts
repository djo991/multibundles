/**
 * Plan name constants used for Shopify billing.
 * Shared between server (shopify.server.ts billing config)
 * and client (billing page UI).
 */
export const PLANS = {
  LAUNCH: "Launch",
  GLOBAL: "Global",
} as const;
